# 🎨 Иконки

PNG-файлы манифеста не хранятся в Git. Они генерируются командой:

```bash
npm run generate:icons
```

Генератор создаёт 16×16, 32×32 и 80×80 версии для основной иконки и команд ленты. `npm run build` вызывает генератор автоматически.
