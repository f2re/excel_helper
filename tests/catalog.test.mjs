import test from 'node:test';import assert from 'node:assert/strict';import { FUNCTION_CATALOG } from '../src/core/function-catalog.js';import { APPLET_CATALOG } from '../src/core/applet-catalog.js';import { TABLE_SPECS } from '../src/schedule/schema.js';test('114 unique functions',()=>{assert.equal(FUNCTION_CATALOG.length,114);assert.equal(new Set(FUNCTION_CATALOG.map(x=>x.id)).size,114)});test('44 executable applets',()=>{assert.equal(APPLET_CATALOG.length,44);assert.ok(APPLET_CATALOG.every(x=>x.action))});test('service tables',()=>assert.ok(Object.keys(TABLE_SPECS).length>=15));
