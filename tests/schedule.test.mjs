import test from 'node:test';import assert from 'node:assert/strict';import { detectProfile,parseScheduleMatrix } from '../src/schedule/parser.js';import { assignSchedule } from '../src/schedule/assigner.js';test('parser profile is editable and parses a lesson',()=>{const m=Array.from({length:30},()=>Array(20).fill(''));m[0][0]='уч. недели';m[0][2]=1;m[3][2]='Л/Т.01';m[4][2]='МАТ';m[5][2]='101';const p=detectProfile(m);assert.equal(p.weekStartCol,2);const r=parseScheduleMatrix(m,{sourceId:'S',group:'522',profile:p});assert.equal(r.lessons.length,1)});test('round robin and strict conflicts',()=>{const lessons=[{lessonId:'L1',week:1,dayOfWeek:'Пн',pairNumber:1,subject:'МАТ',lessonType:'П',room:'1',group:'A'},{lessonId:'L2',week:1,dayOfWeek:'Пн',pairNumber:1,subject:'ФИЗ',lessonType:'П',room:'2',group:'B'}];const candidates=lessons.map(l=>({lessonId:l.lessonId,teacherId:'T1'}));const r=assignSchedule({lessons,candidates,teachers:[{teacherId:'T1',active:true}],strict:true});assert.equal(r.assignments.filter(x=>x.teacherId).length,1);assert.ok(r.conflicts.length)});
