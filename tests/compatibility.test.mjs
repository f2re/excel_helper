import test from 'node:test';import assert from 'node:assert/strict';import { readFile } from 'node:fs/promises';import { chooseCompatibilityMode,COMPATIBILITY_MODES } from '../src/core/compatibility.js';test('compatibility modes',()=>{assert.equal(chooseCompatibilityMode({excelApi17:true,sharedRuntime:true,customFunctions:true}),COMPATIBILITY_MODES.MODERN);assert.equal(chooseCompatibilityMode({excelApi11:true}),COMPATIBILITY_MODES.OFFICE2016_TASKPANE)});test('legacy manifest excludes custom functions',async()=>{const x=await readFile(new URL('../manifest-office2016.xml',import.meta.url),'utf8');assert.match(x,/ExcelApi" MinVersion="1\.1"/);assert.doesNotMatch(x,/CustomFunctions|SharedRuntime/)});
