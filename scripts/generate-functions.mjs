import { writeFile, mkdir } from "node:fs/promises";
import { FUNCTION_CATALOG } from "../src/core/function-catalog.js";
import { APPLET_CATALOG } from "../src/core/applet-catalog.js";
const metadata={$schema:"https://developer.microsoft.com/json-schemas/office-js/custom-functions.schema.json",allowCustomDataForDataTypeAny:true,functions:FUNCTION_CATALOG.map((item)=>({id:item.id,name:item.name,description:item.description,helpUrl:"https://github.com/f2re/excel_helper/blob/main/docs/FUNCTIONS.md",parameters:item.parameters.map((parameter)=>({name:parameter.name,description:parameter.description,type:"any",optional:Boolean(parameter.optional)})),result:{type:"any",dimensionality:item.matrix?"matrix":"scalar"}}))};
await writeFile(new URL("../src/functions.json",import.meta.url),`${JSON.stringify(metadata,null,2)}\n`); await mkdir(new URL("../docs",import.meta.url),{recursive:true});
const categories=new Map();for(const item of FUNCTION_CATALOG){const list=categories.get(item.category)||[];list.push(item);categories.set(item.category,list);} const categoryNames={hr:"👥 Кадры и ФИО",recruiting:"🎯 Рекрутинг",education:"🎓 Преподаватели и занятия",planning:"✅ Планирование",data:"🧹 Данные"}; let functionsDoc=`# ƒ Пользовательские функции\n\nВсего: **${FUNCTION_CATALOG.length}**. Пространство имён Excel: \`PROFI\`.\n\n`;
for(const [category,items] of categories){functionsDoc+=`## ${categoryNames[category]}\n\n| Функция | Назначение | Пример |\n|---|---|---|\n`;for(const item of items)functionsDoc+=`| \`PROFI.${item.name}\` | ${item.description.replace(/\|/g,"\\|")} | \`${item.example.replace(/\|/g,"\\|")}\` |\n`;functionsDoc+="\n";} await writeFile(new URL("../docs/FUNCTIONS.md",import.meta.url),functionsDoc);
let appletsDoc=`# 🧩 Апплеты\n\nВсего: **${APPLET_CATALOG.length}**. Все апплеты доступны через боковую панель и поиск.\n\n| Код | Апплет | Категория | Действие |\n|---|---|---|---|\n`;for(const item of APPLET_CATALOG)appletsDoc+=`| ${item.id} | ${item.title} | ${item.category} | ${item.description.replace(/\|/g,"\\|")} |\n`;await writeFile(new URL("../docs/APPLETS.md",import.meta.url),appletsDoc);console.log(`Generated metadata for ${FUNCTION_CATALOG.length} functions and docs for ${APPLET_CATALOG.length} applets.`);
